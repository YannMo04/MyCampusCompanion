<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ========== PERMISSIONS ========== -->

    <!-- Internet (pour API REST) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Localisation -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Caméra -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Stockage (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        tools:ignore="PhotoAndVideoPolicy" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"
        tools:ignore="PhotoAndVideoPolicy,SelectedPhotoAccess" />

    <!-- Téléphonie -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Fonctionnalités matérielles (optionnelles) -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCampusCompanion"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31"
        tools:ignore="MissingClass">

        <!-- Google Maps API Key (À REMPLACER) -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY_HERE" />

        <!-- MainActivity (activité principale avec BottomNavigationView) -->
        <activity
            android:name=".view.activities.MainActivity"
            android:exported="false"
            tools:ignore="MissingClass" />

        <!-- SplashActivity (écran de démarrage) -->
        <activity
            android:name=".view.activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.SplashScreen"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ActualiteDetailActivity -->
        <activity
            android:name=".view.activities.ActualiteDetailActivity"
            android:exported="false"
            android:parentActivityName=".view.activities.MainActivity"
            tools:ignore="MissingClass" />

        <!-- SignalementDetailActivity -->
        <activity
            android:name=".view.activities.SignalementDetailActivity"
            android:exported="false"
            android:parentActivityName=".view.activities.MainActivity"
            tools:ignore="MissingClass" />

        <activity
            android:name=".view.activities.SignalementListActivity"
            android:exported="false"
            android:parentActivityName=".view.activities.MainActivity" />

        <!-- FileProvider pour partager des fichiers (caméra) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>